# ========================================
# GitLab CI/CD Pipeline Configuration (Optimized)
# ========================================

default:
  image: docker:24-cli

variables:
  # Docker 이미지 설정
  DOCKER_IMAGE_NAME: "ts/${SERVER_NAME}"
  DOCKER_IMAGE_TAG: "latest"

  # Harbor Registry 설정 (선택적)
  HARBOR_IMAGE_NAME: "${HARBOR_REGISTRY}/${HARBOR_PROJECT}/${SERVER_NAME}"

  # Gradle 설정 (최적화)
  GRADLE_OPTS: "-Dorg.gradle.daemon=false -Dorg.gradle.caching=true"
  GRADLE_USER_HOME: $CI_PROJECT_DIR/.gradle

stages:
  - build
  - test
  - docker-build
  - deploy

# ========================================
# 빌드 단계 (최적화)
# ========================================
build-job:
  stage: build
  image: gradle:8.5-jdk17-alpine
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
      tags:
        - main-runner
    - if: '$CI_COMMIT_BRANCH == "dev"'
      tags:
        - dev-runner
  cache:
    # 브랜치별 캐시 사용
    key: gradle-${CI_COMMIT_REF_SLUG}
    paths:
      - .gradle/wrapper/       # Gradle wrapper 캐시
      - .gradle/caches/        # 의존성 캐시
      - build/classes/         # 컴파일된 클래스 캐시
      - build/generated/       # 생성된 소스 캐시
      - build/resources/       # 리소스 캐시
    policy: pull-push
  before_script:
    # Gradle 캐시 디렉토리 생성
    - mkdir -p .gradle
  script:
    - echo "📦 프로젝트 빌드 시작..."
    - gradle build -x test --build-cache --no-daemon
    - echo "✅ 빌드 완료"
  artifacts:
    paths:
      - build/libs/*.jar
    expire_in: 1 hour

# ========================================
# 테스트 단계
# ========================================
test-job:
  stage: test
  image: gradle:8.5-jdk17-alpine
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
      tags:
        - main-runner
    - if: '$CI_COMMIT_BRANCH == "dev"'
      tags:
        - dev-runner
  cache:
    key: gradle-${CI_COMMIT_REF_SLUG}
    paths:
      - .gradle/wrapper/
      - .gradle/caches/
    policy: pull
  script:
    - echo "🧪 테스트 실행 중..."
    - gradle test --build-cache --no-daemon
    - echo "✅ 테스트 완료"
  artifacts:
    when: always
    reports:
      junit: build/test-results/test/TEST-*.xml
    paths:
      - build/test-results/
      - build/reports/
    expire_in: 1 weeks
  coverage: '/Total.*?([0-9]{1,3})%/'
  when: manual

# ========================================
# Docker 이미지 빌드 단계 (BuildKit 캐시 활용)
# ========================================
docker-build-job:
  stage: docker-build
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
      tags:
        - main-runner
    - if: '$CI_COMMIT_BRANCH == "dev"'
      tags:
        - dev-runner
  dependencies:
    - build-job
  variables:
    DOCKER_BUILDKIT: "1"
    BUILDKIT_PROGRESS: "plain"
  before_script:
    - echo "$HARBOR_PASSWORD" | docker login http://$HARBOR_REGISTRY -u $HARBOR_USERNAME --password-stdin || echo "⚠️  Harbor 로그인 실패"
  script:
    - |
      echo "=== Harbor 이미지 존재 확인 ==="
      HARBOR_IMAGE_SHA="${HARBOR_REGISTRY}/${HARBOR_PROJECT}/${SERVER_NAME}:${CI_COMMIT_SHORT_SHA}"

      if docker manifest inspect ${HARBOR_IMAGE_SHA} > /dev/null 2>&1; then
        echo "✓ Harbor에 이미지 존재: ${HARBOR_IMAGE_SHA}"
        echo "⏭️  빌드 스킵"
        exit 0
      fi

      echo "❌ Harbor에 이미지 없음 → 빌드 시작..."
      docker build \
        -f docker/Dockerfile.ci \
        -t ${DOCKER_IMAGE_NAME}:${DOCKER_IMAGE_TAG} \
        --build-arg BUILDKIT_INLINE_CACHE=1 \
        --cache-from ${DOCKER_IMAGE_NAME}:latest \
        --progress=plain \
        --force-rm \
        .

    - 'echo "✅ 이미지 빌드 완료: ${DOCKER_IMAGE_NAME}:${DOCKER_IMAGE_TAG}"'
    - docker images | grep ${DOCKER_IMAGE_NAME}

# ========================================
# Harbor Push 단계 (deploy와 동시 실행)
# ========================================
harbor-push-job:
  stage: deploy
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
      tags:
        - main-runner
    - if: '$CI_COMMIT_BRANCH == "dev"'
      tags:
        - dev-runner
  dependencies:
    - docker-build-job
  before_script:
    - echo "🔑 Harbor 로그인 중..."
    - echo "$HARBOR_PASSWORD" | docker login http://$HARBOR_REGISTRY -u $HARBOR_USERNAME --password-stdin
  script:
    - |
      echo "=== Harbor Push 확인 ==="
      VERSION=${VERSION:-$CI_COMMIT_SHORT_SHA}
      echo "📦 사용할 버전: ${VERSION}"
      LOCAL_IMAGE="${DOCKER_IMAGE_NAME}:${DOCKER_IMAGE_TAG}"

      if docker images --format "{{.Repository}}:{{.Tag}}" | grep -q "^${LOCAL_IMAGE}$"; then
        echo "✓ 로컬 이미지 존재 → Harbor에 Push 시작"

        echo "🏷️  이미지 태깅..."
        docker tag ${LOCAL_IMAGE} ${HARBOR_IMAGE_NAME}:${VERSION}
        docker tag ${LOCAL_IMAGE} ${HARBOR_IMAGE_NAME}:latest

        echo "⬆️  Harbor에 Push 중..."
        docker push ${HARBOR_IMAGE_NAME}:${VERSION}
        docker push ${HARBOR_IMAGE_NAME}:latest

        echo "🗑️  호스트에서 Harbor 태그 이미지 삭제..."
        docker rmi ${HARBOR_IMAGE_NAME}:${VERSION} || true
        docker rmi ${HARBOR_IMAGE_NAME}:latest || true

        echo "✅ 이미지가 Harbor에 성공적으로 등록되었습니다!"
        echo "  - ${HARBOR_IMAGE_NAME}:${VERSION}"
        echo "  - ${HARBOR_IMAGE_NAME}:latest"
        echo "✅ 호스트에는 ${LOCAL_IMAGE}만 유지됩니다"
      else
        echo "⏭️  로컬 이미지 없음 (빌드 스킵됨) → Push 스킵"
        echo "ℹ️  docker-build-job이 실행되지 않았거나 이미 Harbor에 이미지가 존재합니다"
      fi
  after_script:
    - docker logout $HARBOR_REGISTRY || true

# ========================================
# 배포 단계
# ========================================
deploy-job:
  stage: deploy
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
      tags:
        - main-runner
    - if: '$CI_COMMIT_BRANCH == "dev"'
      tags:
        - dev-runner
  dependencies:
    - build-job
    - docker-build-job
  before_script:
    - |
      echo "=== 필수 도구 설치 중 ==="
      apk add --no-cache curl 2>/dev/null || apt-get update && apt-get install -y curl 2>/dev/null || yum install -y curl 2>/dev/null || true
      command -v curl && echo "✓ curl 설치됨" || echo "✗ curl 설치 실패"
    - |
      echo "=== Gitlab Variables로 .env 파일 생성 중 ==="
      env | \
        grep -vE '^(CI_|GITLAB_|FF_|RUNNER_|DOCKER_|DIND_|PATH=|HOME=|HOSTNAME=|PWD=|OLDPWD=|SHELL=|SHLVL=|USER=|_=|LANG=|LC_|TERM=|DEBIAN_|GRADLE_|GIT_)' | \
        grep -v '^CI=' | \
        grep -E '^[A-Z_]+=.+$' > .env

      # Docker 환경 설정 추가
      echo "DOCKER_IMAGE=${DOCKER_IMAGE_NAME}:${DOCKER_IMAGE_TAG}" >> .env
      echo "RELEASE_BASE_PATH=/app/release_files" >> .env

      echo "✅ .env 파일 생성 완료:"
      cat .env
    - |
      echo "=== Docker 네트워크 확인 ==="
      docker network ls | grep -q network-ts && echo "✓ network-ts 존재" || (docker network create network-ts && echo "✓ network-ts 생성")
    - |
      echo "=== Harbor 로그인 ==="
      echo "$HARBOR_PASSWORD" | docker login http://$HARBOR_REGISTRY -u $HARBOR_USERNAME --password-stdin || echo "⚠️  Harbor 로그인 실패"
  script:
    - |
      echo "=== App 이미지 확인 ==="
      LOCAL_IMAGE="ts/${SERVER_NAME}:latest"

      if ! docker images --format "{{.Repository}}:{{.Tag}}" | grep -q "^${LOCAL_IMAGE}$"; then
        echo "❌ 로컬 이미지가 존재하지 않습니다: ${LOCAL_IMAGE}"
        echo "   docker-build-job이 실패했습니다"
        exit 1
      fi

      echo "✓ 로컬 이미지 확인: ${LOCAL_IMAGE}"

    # JAR에서 리소스 파일 추출 및 복사 (기존 파일 보존, 없는 파일만 복사)
    - |
      echo "=== 리소스 파일 동기화 ==="
      RELEASE_DIR="/data/release_manager/release_files"
      JAR_FILE=$(ls build/libs/*.jar | head -1)
      TEMP_EXTRACT_DIR="/tmp/release_extract_$$"

      if [ -f "$JAR_FILE" ]; then
        echo "📦 JAR 파일에서 리소스 추출 중: $JAR_FILE"
        mkdir -p ${TEMP_EXTRACT_DIR}

        # JAR에서 release 디렉토리만 추출
        unzip -q -o "$JAR_FILE" "BOOT-INF/classes/release/*" -d ${TEMP_EXTRACT_DIR} 2>/dev/null || true

        if [ -d "${TEMP_EXTRACT_DIR}/BOOT-INF/classes/release" ]; then
          echo "📂 리소스 파일 동기화 중 (기존 파일 보존)..."

          # rsync로 없는 파일만 복사 (--ignore-existing: 기존 파일 덮어쓰지 않음)
          if command -v rsync > /dev/null 2>&1; then
            rsync -av --ignore-existing ${TEMP_EXTRACT_DIR}/BOOT-INF/classes/release/ ${RELEASE_DIR}/
          else
            # rsync 없으면 cp로 대체 (디렉토리 구조 유지하며 복사)
            cd ${TEMP_EXTRACT_DIR}/BOOT-INF/classes/release
            find . -type f | while read file; do
              dest="${RELEASE_DIR}/${file}"
              if [ ! -f "$dest" ]; then
                mkdir -p "$(dirname "$dest")"
                cp "$file" "$dest"
                echo "  복사: $file"
              fi
            done
            cd - > /dev/null
          fi

          echo "✅ 리소스 파일 동기화 완료"
        else
          echo "⚠️  JAR에서 release 디렉토리를 찾을 수 없습니다"
        fi

        # 임시 디렉토리 정리
        rm -rf ${TEMP_EXTRACT_DIR}
      else
        echo "⚠️  JAR 파일을 찾을 수 없습니다"
      fi

      chmod -R 777 ${RELEASE_DIR}
      echo "📁 최종 디렉토리 구조:"
      ls -laR ${RELEASE_DIR}/ | head -50 || true

    - |
      echo "=== 기존 컨테이너 정리 ==="
      docker-compose -p release_manager -f docker/docker-compose.yml rm -f mariadb redis app || true

    - |
      echo "=== MariaDB 시작 ==="
      if docker ps --format '{{.Names}}' | grep -q '^release-manager-mariadb$'; then
        echo "✓ MariaDB 실행 중"
      elif docker ps -a --format '{{.Names}}' | grep -q '^release-manager-mariadb$'; then
        docker start release-manager-mariadb && echo "✓ MariaDB 재시작됨"
      else
        docker-compose -p release_manager -f docker/docker-compose.yml up -d mariadb && echo "✓ MariaDB 생성됨"
      fi

    - |
      echo "=== Redis 시작 ==="
      if docker ps --format '{{.Names}}' | grep -q '^release-manager-redis$'; then
        echo "✓ Redis 실행 중"
      elif docker ps -a --format '{{.Names}}' | grep -q '^release-manager-redis$'; then
        docker start release-manager-redis && echo "✓ Redis 재시작됨"
      else
        docker-compose -p release_manager -f docker/docker-compose.yml up -d redis && echo "✓ Redis 생성됨"
      fi

    - |
      echo "=== MariaDB 헬스체크 ==="
      max_attempts=60
      attempt=0
      until docker exec release-manager-mariadb healthcheck.sh --connect --innodb_initialized || [ $attempt -eq $max_attempts ]; do
        attempt=$((attempt+1))
        echo "  MariaDB 대기 중... ($attempt/$max_attempts)"
        sleep 3
      done
      if [ $attempt -eq $max_attempts ]; then
        echo "✗ MariaDB 헬스체크 실패"
        docker logs --tail 20 release-manager-mariadb
        exit 1
      fi
      echo "✓ MariaDB 준비 완료 (${attempt}회 시도)"

    - |
      echo "=== Redis 헬스체크 ==="
      max_attempts=60
      attempt=0
      until docker exec release-manager-redis redis-cli -a "${REDIS_PASSWORD}" ping 2>/dev/null | grep -q PONG || [ $attempt -eq $max_attempts ]; do
        attempt=$((attempt+1))
        echo "  Redis 대기 중... ($attempt/$max_attempts)"
        sleep 3
      done
      if [ $attempt -eq $max_attempts ]; then
        echo "✗ Redis 헬스체크 실패"
        echo "=== Redis 로그 ==="
        docker logs --tail 20 release-manager-redis
        echo "=== Redis 연결 테스트 ==="
        docker exec release-manager-redis redis-cli -a "${REDIS_PASSWORD}" ping 2>&1 || true
        exit 1
      fi
      echo "✓ Redis 준비 완료 (${attempt}회 시도)"

    - |
      echo "=== 애플리케이션 배포 ==="
      docker stop ${SERVER_NAME} || true
      docker rm -f ${SERVER_NAME} || true
      docker-compose -p release_manager -f docker/docker-compose.yml up -d --no-deps app
      echo "✓ 컨테이너 시작됨"
      sleep 10

    - |
      echo "=== 애플리케이션 헬스체크 ==="
      max_attempts=60
      attempt=0
      until curl -sf http://${SERVER_HOST}:${SERVER_EXTERNAL_PORT}/actuator/health > /dev/null || [ $attempt -eq $max_attempts ]; do
        attempt=$((attempt+1))
        echo "  애플리케이션 대기 중... ($attempt/$max_attempts)"
        sleep 3
      done
      if [ $attempt -eq $max_attempts ]; then
        echo "✗ 애플리케이션 헬스체크 실패 (3분 타임아웃)"
        echo "=== 컨테이너 로그 ==="
        docker logs --tail 50 ${SERVER_NAME}
        echo "=== 컨테이너 상태 ==="
        docker ps -a --filter "name=${SERVER_NAME}"
        exit 1
      fi
      echo "✅ 애플리케이션 준비 완료 (${attempt}회 시도, $((attempt * 3))초 소요)"
      echo "=== 배포 성공 ==="

  after_script:
    - echo "=== 정리 작업 ==="
    - docker image prune -f --filter "label=project=${SERVER_NAME}" 2>/dev/null || true
    - echo "✓ ${SERVER_NAME} Dangling 이미지 정리 완료"

    - echo "=== 배포 상태 ==="
    - docker ps --filter "name=${SERVER_NAME}" --format "table {{.Names}}\t{{.Image}}\t{{.Status}}\t{{.Ports}}"
    - docker images --filter "reference=${DOCKER_IMAGE_NAME}" --format "table {{.Repository}}\t{{.Tag}}\t{{.ID}}\t{{.Size}}"

  environment:
    name: production
    url: http://${SERVER_HOST}:${SERVER_EXTERNAL_PORT}
