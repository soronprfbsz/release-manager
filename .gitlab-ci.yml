# ========================================
# GitLab CI/CD Pipeline Configuration (Optimized)
# ========================================

default:
  image: docker:24-cli

variables:
  # Docker 이미지 설정
  DOCKER_IMAGE_NAME: "ts/${SERVER_NAME}"
  DOCKER_IMAGE_TAG: "latest"

  # Harbor Registry 설정 (선택적)
  HARBOR_IMAGE_NAME: "${HARBOR_HOST}/${HARBOR_PROJECT}/${SERVER_NAME}"

  # Gradle 설정 (최적화)
  GRADLE_OPTS: "-Dorg.gradle.daemon=false -Dorg.gradle.caching=true"
  GRADLE_USER_HOME: $CI_PROJECT_DIR/.gradle

stages:
  - build
  - test
  - docker-build
  - harbor-push
  - deploy

# ========================================
# 빌드 단계 (최적화)
# ========================================
build-job:
  stage: build
  image: gradle:8.5-jdk17-alpine
  tags:
    - 17-runner
  only:
    - main
  cache:
    # 브랜치별 캐시 사용
    key: gradle-${CI_COMMIT_REF_SLUG}
    paths:
      - .gradle/wrapper/       # Gradle wrapper 캐시
      - .gradle/caches/        # 의존성 캐시
      - build/classes/         # 컴파일된 클래스 캐시
      - build/generated/       # 생성된 소스 캐시
      - build/resources/       # 리소스 캐시
    policy: pull-push
  before_script:
    # Gradle 캐시 디렉토리 생성
    - mkdir -p .gradle
  script:
    - echo "📦 프로젝트 빌드 시작..."
    - gradle build -x test --build-cache --no-daemon
    - echo "✅ 빌드 완료"
  artifacts:
    paths:
      - build/libs/*.jar
    expire_in: 1 hour

# ========================================
# 테스트 단계
# ========================================
test-job:
  stage: test
  image: gradle:8.5-jdk17-alpine
  tags:
    - 17-runner
  only:
    - main
  cache:
    key: gradle-${CI_COMMIT_REF_SLUG}
    paths:
      - .gradle/wrapper/
      - .gradle/caches/
    policy: pull
  script:
    - echo "🧪 테스트 실행 중..."
    - gradle test --build-cache --no-daemon
    - echo "✅ 테스트 완료"
  artifacts:
    when: always
    reports:
      junit: build/test-results/test/TEST-*.xml
    paths:
      - build/test-results/
      - build/reports/
    expire_in: 1 weeks
  coverage: '/Total.*?([0-9]{1,3})%/'
  when: manual

# ========================================
# Docker 이미지 빌드 단계 (BuildKit 캐시 활용)
# ========================================
docker-build-job:
  stage: docker-build
  tags:
    - 17-runner
  only:
    - main
  dependencies:
    - build-job
  variables:
    # BuildKit 활성화 (레이어 캐시 최적화)
    DOCKER_BUILDKIT: "1"
    BUILDKIT_PROGRESS: "plain"
  script:
    - echo "🐳 Docker 이미지 빌드 시작..."
    # BuildKit 캐시를 사용한 빌드
    - |
      docker build \
        -f docker/Dockerfile.ci \
        -t ${DOCKER_IMAGE_NAME}:${DOCKER_IMAGE_TAG} \
        --build-arg BUILDKIT_INLINE_CACHE=1 \
        --cache-from ${DOCKER_IMAGE_NAME}:latest \
        --progress=plain \
        --force-rm \
        .

    - 'echo "✅ 이미지 빌드 완료: ${DOCKER_IMAGE_NAME}:${DOCKER_IMAGE_TAG}"'
    - docker images | grep ${DOCKER_IMAGE_NAME}

# ========================================
# Harbor Push 단계
# ========================================
harbor-push-job:
  stage: harbor-push
  tags:
    - 17-runner
  only:
    - main
  dependencies:
    - docker-build-job
  before_script:
    - echo "🔑 Harbor 로그인 중..."
    - echo "$HARBOR_PASSWORD" | docker login $HARBOR_HOST -u $HARBOR_USERNAME --password-stdin
  script:
    - echo "🏷️  로컬 이미지를 Harbor 형식으로 태깅..."
    - docker tag ${DOCKER_IMAGE_NAME}:${DOCKER_IMAGE_TAG} ${HARBOR_IMAGE_NAME}:${CI_COMMIT_SHORT_SHA}
    - docker tag ${DOCKER_IMAGE_NAME}:${DOCKER_IMAGE_TAG} ${HARBOR_IMAGE_NAME}:latest

    - echo "⬆️  Harbor에 이미지 Push 중..."
    - docker push ${HARBOR_IMAGE_NAME}:${CI_COMMIT_SHORT_SHA}
    - docker push ${HARBOR_IMAGE_NAME}:latest

    - echo "✅ 이미지가 Harbor에 성공적으로 등록되었습니다!"
    - echo "  - ${HARBOR_IMAGE_NAME}:${CI_COMMIT_SHORT_SHA}"
    - echo "  - ${HARBOR_IMAGE_NAME}:latest"
  after_script:
    - docker logout $HARBOR_HOST || true
  when: manual

# ========================================
# 배포 단계
# ========================================
deploy-job:
  stage: deploy
  tags:
    - 17-runner
  only:
    - main
  dependencies:
    - docker-build-job
  before_script:
    - |
      echo "=== 필수 도구 설치 중 ==="
      apk add --no-cache curl 2>/dev/null || apt-get update && apt-get install -y curl 2>/dev/null || yum install -y curl 2>/dev/null || true
      command -v curl && echo "✓ curl 설치됨" || echo "✗ curl 설치 실패"
    - |
      echo "=== Gitlab Variables로 .env 파일 생성 중 ==="
      env | grep -vE '^(CI=|CI_|GITLAB_|FF_|RUNNER_|DOCKER_|DIND_|PATH=|HOME=|HOSTNAME=|PWD=|OLDPWD=|SHELL=|SHLVL=|USER=|_=|LANG=|LC_|TERM=|DEBIAN_|GRADLE_)' > .env

      # Docker 이미지 정보 추가
      echo "DOCKER_IMAGE=${DOCKER_IMAGE_NAME}:${DOCKER_IMAGE_TAG}" >> .env

      cat .env
      cp .env docker/.env
    - |
      echo "=== Docker 네트워크 확인 ==="
      docker network ls | grep -q network-ts && echo "✓ network-ts 존재" || (docker network create network-ts && echo "✓ network-ts 생성")
  script:
    # 초기 릴리즈 파일 복사
    - |
      echo "=== 릴리즈 파일 초기화 ==="
      mkdir -p /data/release_manager/release_files
      if [ ! -f /data/release_manager/release_files/.initialized ]; then
        if [ -d "/data/release_manager/release_files/releases" ]; then
          echo "⚠️  기존 파일 백업 중..."
          tar czf "/data/release_manager/release_files_backup_$(date +%Y%m%d_%H%M%S).tar.gz" \
                  -C /data/release_manager release_files
        fi
        echo "📦 초기 릴리즈 파일 복사 중..."
        cp -r src/main/resources/release/* /data/release_manager/release_files/
        touch /data/release_manager/release_files/.initialized
        echo "✅ 초기화 완료"
      else
        echo "ℹ️  릴리즈 파일 이미 초기화됨"
      fi

    - |
      echo "=== 기존 컨테이너 정리 ==="
      docker-compose -p release_manager -f docker/docker-compose.yml rm -f mariadb redis app || true

    - |
      echo "=== MariaDB 시작 ==="
      if docker ps --format '{{.Names}}' | grep -q '^rm-mariadb$'; then
        echo "✓ MariaDB 실행 중"
      elif docker ps -a --format '{{.Names}}' | grep -q '^rm-mariadb$'; then
        docker start release-manager-mariadb && echo "✓ MariaDB 재시작됨"
      else
        docker-compose -p release_manager -f docker/docker-compose.yml up -d mariadb && echo "✓ MariaDB 생성됨"
      fi

    - |
      echo "=== Redis 시작 ==="
      if docker ps --format '{{.Names}}' | grep -q '^rm-redis$'; then
        echo "✓ Redis 실행 중"
      elif docker ps -a --format '{{.Names}}' | grep -q '^rm-redis$'; then
        docker start release-manager-redis && echo "✓ Redis 재시작됨"
      else
        docker-compose -p release_manager -f docker/docker-compose.yml up -d redis && echo "✓ Redis 생성됨"
      fi

    - |
      echo "=== MariaDB 헬스체크 ==="
      max_attempts=30
      attempt=0
      until docker exec release-manager-mariadb healthcheck.sh --connect --innodb_initialized || [ $attempt -eq $max_attempts ]; do
        attempt=$((attempt+1))
        sleep 2
      done
      if [ $attempt -eq $max_attempts ]; then
        echo "✗ MariaDB 헬스체크 실패"
        docker logs --tail 20 release-manager-mariadb
        exit 1
      fi
      echo "✓ MariaDB 준비 완료"

    - |
      echo "=== Redis 헬스체크 ==="
      max_attempts=30
      attempt=0
      until docker exec release-manager-redis redis-cli ping || [ $attempt -eq $max_attempts ]; do
        attempt=$((attempt+1))
        sleep 2
      done
      if [ $attempt -eq $max_attempts ]; then
        echo "✗ Redis 헬스체크 실패"
        docker logs --tail 20 release-manager-redis
        exit 1
      fi
      echo "✓ Redis 준비 완료"

    - |
      echo "=== 애플리케이션 배포 ==="
      docker stop ${SERVER_NAME} || true
      docker rm -f ${SERVER_NAME} || true
      docker-compose -p release_manager -f docker/docker-compose.yml up -d --no-deps app
      echo "✓ 컨테이너 시작됨"
      sleep 10

    - |
      echo "=== 애플리케이션 헬스체크 ==="
      max_attempts=30
      attempt=0
      until curl -sf http://${SERVER_HOST}:${SERVER_EXTERNAL_PORT}/actuator/health > /dev/null || [ $attempt -eq $max_attempts ]; do
        attempt=$((attempt+1))
        echo "  대기 중... ($attempt/$max_attempts)"
        sleep 2
      done
      if [ $attempt -eq $max_attempts ]; then
        echo "✗ 애플리케이션 헬스체크 실패"
        docker logs ${SERVER_NAME}
        exit 1
      fi
      echo "✅ 애플리케이션 준비 완료"
      echo "=== 배포 성공 ==="

  after_script:
    - echo "=== 정리 작업 ==="
    - docker images -f "dangling=true" -f "label=project=${SERVER_NAME}" -q | xargs -r docker rmi -f 2>/dev/null || true
    - docker image prune -f --filter "label=project=${SERVER_NAME}" --filter "dangling=true" 2>/dev/null || true
    - echo "✓ Dangling 이미지 정리 완료"

    - echo "=== 배포 상태 ==="
    - docker ps --filter "name=${SERVER_NAME}" --format "table {{.Names}}\t{{.Image}}\t{{.Status}}\t{{.Ports}}"
    - docker images --filter "reference=${DOCKER_IMAGE_NAME}" --format "table {{.Repository}}\t{{.Tag}}\t{{.ID}}\t{{.Size}}"

  environment:
    name: production
    url: http://${SERVER_HOST}:${SERVER_EXTERNAL_PORT}
