package com.ts.rm.domain.releasefile.entity;

/**
 * ReleaseFile Entity 테스트
 *
 * Note: isExcludedFromPatch() 메서드가 제거되었으므로 관련 테스트도 삭제됨.
 * 패치 제외 로직은 이제 ReleaseVersion의 releaseCategory로 처리됨.
 */
class ReleaseFileTest {
    // 현재 ReleaseFile 엔티티에 테스트가 필요한 비즈니스 로직이 없음
    // 필요 시 추가
}
