# ========================================
# Server Configuration
# ========================================
SERVER_NAME=release-manager-api
SERVER_HOST=localhost
SERVER_ENVIRONMENT=dev
SERVER_PORT=8081
SERVER_EXTERNAL_PORT=18080
RELEASE_BASE_PATH="src/main/resources/release"

# ========================================
# MariaDB Configuration
# ========================================
MARIADB_HOST=localhost
MARIADB_PORT=13306
MARIADB_DATABASE=release_manager
MARIADB_ROOT_PASSWORD="your_root_password_here"
MARIADB_USERNAME=ts_dev
MARIADB_PASSWORD="your_password_here"

# Database Connection Pool
MARIADB_POOL_SIZE=10
MARIADB_POOL_MIN_IDLE=5
MARIADB_CONNECTION_TIMEOUT=30000

# ========================================
# Docker Configuration
# ========================================
# Docker 이미지 이름 (CI/CD에서 자동 설정됨)
DOCKER_IMAGE=ts/spring-boilerplate:latest


# ========================================
# Flyway Configuration
# ========================================
# Flyway 활성화 여부 (true: 마이그레이션 수행, false: 비활성화)
FLYWAY_ENABLED=true

# ========================================
# Redis Configuration
# ========================================
# Redis Host
# - Windows 로컬 프로세스: localhost
# - Docker 컨테이너 내부: redis
REDIS_HOST=localhost
REDIS_PORT=16379
REDIS_PASSWORD="your_redis_password_here"

# ========================================
# gRPC Configuration
# ========================================
GRPC_HOST=localhost
GRPC_PORT=9090
GRPC_TIMEOUT=30
GRPC_MAX_RETRY=3

# ========================================
# GitLab CI/CD Variables
# ========================================
# CI/CD 파이프라인에서 자동으로 .env 파일을 생성하기 위해
# 아래 변수들을 GitLab의 Settings → CI/CD → Variables에 등록해야 합니다.
#
# 등록 방법:
# 1. GitLab 프로젝트 페이지 이동
# 2. Settings → CI/CD → Variables 섹션 확장
# 3. "Add variable" 클릭하여 아래 변수들을 하나씩 추가
#
# 주의사항:
# - 특수문자(!, #, $, & 등) 포함 비밀번호는 "Expand variable reference" 체크 해제 필수
# - Masked 옵션: 민감한 정보(비밀번호 등)는 활성화 권장
# - Protected 옵션: Protected 브랜치에서만 사용 시 활성화
#
# ========================================
# 필수 Variables 목록:
# ========================================
#
# [서버 설정]
# SERVER_NAME=release-manager-api
# SERVER_HOST=your_host
# SERVER_PORT=your_server_port
# SERVER_EXTERNAL_PORT=your_docker_external_port
# RELEASE_BASE_PATH="/app/release_files"
#
# [MariaDB 설정]
# MARIADB_DATABASE=release_manager
# MARIADB_ROOT_PASSWORD=your_root_password_here
# MARIADB_USERNAME=your_username_here
# MARIADB_PASSWORD=your_password_here
# MARIADB_PORT=your_mariadb_port
# MARIADB_POOL_SIZE=10
# MARIADB_POOL_MIN_IDLE=5
# MARIADB_CONNECTION_TIMEOUT=30000
#
# [Flyway 설정]
# FLYWAY_ENABLED=true
#
# [Redis 설정]
# REDIS_HOST=redis
# REDIS_PORT=your_redis_port
# REDIS_PASSWORD=your_redis_password_here
#
# [gRPC 설정]
# GRPC_HOST=localhost
# GRPC_PORT=9090
# GRPC_TIMEOUT=30
# GRPC_MAX_RETRY=3
#
# [Harbor Registry 설정 - 선택사항]
# HARBOR_REGISTRY=your_harbor_host:your_harbor_port
# HARBOR_PROJECT=your_harbor_project
# HARBOR_USERNAME=your_harbor_username
# HARBOR_PASSWORD=your_harbor_password
